describe("Authentication Service", () => {
  it("should Login successfully when email and password are correct", () => {
    expect(1).toBe(1);
  });
  it("should error if email or password is not correct ", async () => {
    expect(1).toBe(1);
  });
  it("should Login successfully when email and password are correct", () => {
    expect(1).toBe(1);
  });
  it("should error if email or password is not correct ", async () => {
    expect(1).toBe(1);
  });
  it("should Login successfully when email and password are correct", () => {
    expect(1).toBe(2);
  });
  it("should error if email or password is not correct ", async () => {
    expect(1).toBe(1);
  });
  it("should Login successfully when email and password are correct", () => {
    expect(1).toBe(1);
  });
  it("should error if email or password is not correct ", async () => {
    expect(1).toBe(1);
  });
});
