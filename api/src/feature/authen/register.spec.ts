describe("Authentication Service", () => {
  it("should Register successfully when all require field are sent", () => {
    expect(1).toBe(1);
  });
  it("should error if one of require field is not sent", async () => {
    expect(1).toBe(1);
  });
  it("should Register successfully when all require field are sent", () => {
    expect(1).toBe(1);
  });
  it("should error if one of require field is not sent", async () => {
    expect(1).toBe(1);
  });
  it("should Register successfully when all require field are sent", () => {
    expect(1).toBe(1);
  });
  it("should error if one of require field is not sent", async () => {
    expect(1).toBe(1);
  });
  it("should Register successfully when all require field are sent", () => {
    expect(1).toBe(1);
  });
  it("should error if one of require field is not sent", async () => {
    expect(1).toBe(1);
  });
});
